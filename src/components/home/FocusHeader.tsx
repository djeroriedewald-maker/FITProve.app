import React from 'react';
// This file remains minimal because Home already renders the header.
// Kept for parity if you want to move the header out of Home again.
export default function FocusHeader() {
  return null;
}
